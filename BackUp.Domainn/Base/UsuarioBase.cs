
using BackUp.Domainn.Base;

namespace BackUp.Domainn.Base
{
    public abstract class UsuarioBase : AuditEntity
    {
       // public string? Departamento { get; set; }
       // public List<string> Permisos { get; set; } = new();

        // Métodos comunes para todos los usuarios
       // public bool TienePermiso(string permiso) => Permisos.Contains(permiso);
       // public void AgregarPermiso(string permiso) => Permisos.Add(permiso);
    }
}